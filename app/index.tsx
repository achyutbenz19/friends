import Camera from "@/components/Camera";
import { View } from "react-native";

export default function Index() {
  return (
    <View
      className="h-full"
    >
      <Camera />
    </View>
  );
}
